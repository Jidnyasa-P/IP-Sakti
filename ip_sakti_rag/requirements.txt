# =============================================================================
# IP-SAKTI RAG microservice — Python dependencies
# Deliberately excludes torch / onnxruntime / sentence-transformers:
# embeddings are done via the Gemini API (see embeddings.py) to stay inside
# Render's free-tier 512MB RAM limit. See SETUP_GUIDE.md "Why no local model".
# =============================================================================
uvicorn[standard]==0.30.6

# Vector DB (Qdrant Cloud)
qdrant-client==1.11.3

# Knowledge graph (Neo4j Aura)
neo4j==5.24.0

# Lexical search (pure-python, no compiled deps — fine for a corpus of a few
# hundred to a few thousand statutory chunks)
rank-bm25==0.2.2

# App database
pymongo==4.9.1

# Embeddings + generation (Google Gemini — text-embedding-004 + gemini-1.5-flash)
google-generativeai==0.8.3

# HTTP calls (Bhashini pipeline API, internal calls from/to the Node backend)
httpx==0.27.2
tenacity==9.0.0

# --- Core ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Retrieval ---
sentence-transformers>=2.6  # multilingual embeddings + optional cross-encoder reranker
numpy>=1.26

# --- Document ingestion ---
pypdf>=4.1
beautifulsoup4>=4.12
lxml>=5.1
requests>=2.31          # used by scripts/download_static_sources.py
pytesseract>=0.3.10      # OCR fallback for scanned/image-only PDFs (also needs the
pdf2image>=1.17          # tesseract-ocr and poppler-utils system packages installed)

# --- Grounded LLM generation (free-tier) ---
google-generativeai>=0.5    # optional: only used if LLM_API_KEY is set

# --- FastAPI-side (only needed if you run the included example server) ---
fastapi>=0.110