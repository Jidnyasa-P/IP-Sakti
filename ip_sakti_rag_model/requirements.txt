# --- Core ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Retrieval ---
rank-bm25>=0.2.2
qdrant-client>=1.9          # supports local on-disk mode (path=...), no server required
sentence-transformers>=2.6  # multilingual embeddings + optional cross-encoder reranker
numpy>=1.26

# --- Document ingestion ---
pypdf>=4.1
beautifulsoup4>=4.12
lxml>=5.1

# --- Grounded LLM generation (free-tier) ---
google-generativeai>=0.5    # optional: only used if GEMINI_API_KEY is set

# --- Optional knowledge graph (only imported if NEO4J_ENABLED=true) ---
neo4j>=5.19

# --- FastAPI-side (only needed if you run the included example server) ---
fastapi>=0.110
uvicorn>=0.29
